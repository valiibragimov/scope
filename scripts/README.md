# Build And Maintenance Scripts

This directory contains repository scripts for building generated client files, injecting local Firebase configuration, preparing BIM viewer assets, running smoke tests, and maintaining runtime artifacts.

Scripts should be deterministic and safe to run from a clean checkout.
