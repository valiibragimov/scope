# GitHub Automation

This directory contains GitHub Actions workflows for Tehnadzor.

- `ci.yml` runs repository checks for the client and tests.
- `refresh-profile-feed.yml` refreshes generated profile feed data.

Keep workflow changes small and verify that they do not expose secrets, Firebase credentials, service account files, or project data.
