# Firebase Functions

This directory contains Firebase Functions code and build configuration.

Functions are used for server-side workflows that should not run directly in the browser. Keep secrets in Firebase or environment configuration, not in source files.
