# VS Code Settings

This directory stores workspace settings for local development.

The settings are intentionally minimal and should only contain editor behavior that is useful for the project as a whole. User-specific extensions, private paths, credentials, and machine-specific configuration should stay out of the repository.
