# Local Server

This directory contains the local Node/Express API used by Tehnadzor during development and server-side experiments.

Keep API behavior aligned with the client data model and avoid committing local credentials, object data, or private integration settings.
